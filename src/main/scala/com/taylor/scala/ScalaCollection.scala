package com.taylor.scala

import scala.collection.mutable.{ArrayBuffer, ListBuffer}



/**
  * @description: ${description}
  * @author: Taylor
  * @date :  2020-11-09 14:37
  **/
object ScalaCollection {

  def main(args: Array[String]): Unit = {


//    val listBuffer: ListBuffer[String] = ListBuffer[String]("")
//    val size: Int = listBuffer.count(s=>{s.length>2})
//
//    listBuffer.append()
//    val list = List[String]("hello java","hello scala","hello spark")
//
//    val strings: List[String] = list.filter(_.equals("hello java"))
//    list.foreach(println)
//    val mapResult: List[Array[String]] = list.map(s=>{s.split(" ")})
//    mapResult.foreach(arr => {arr.foreach(println)})


//    println("--------------------")
//    val flatmapResult: List[String] = list.flatMap(s=>{s.split(" ")})
//    flatmapResult.foreach(println)

/*    //不可变
    var arr0 = Array[Int](1,2,3)
    arr0.foreach(println)

    //可变
    var arr = ArrayBuffer[Int](1,2)
    arr.+=(3)
    arr.+=:(50)
    arr.append(4,5,6)
    arr.foreach(println)*/



  }
}
